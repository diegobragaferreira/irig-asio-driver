[workspace]
members = [
    "irig_asio",
    "tools/asio_probe",
    "tools/installer",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = false

[profile.dev]
panic = "abort"
